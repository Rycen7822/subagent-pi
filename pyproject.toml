[project]
name = "subagent-pi-codex"
version = "0.1.0"
description = "Local persistent subagent-pi via MCP and CLI"
requires-python = ">=3.11"
dependencies = []
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]
